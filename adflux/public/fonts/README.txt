Fonts bundled for PDF export.

These TTFs are loaded by @react-pdf/renderer at PDF generation time.
Do NOT delete them — PDF export depends on both files being present
at the exact paths:
    /fonts/Roboto-Regular.ttf
    /fonts/Roboto-Bold.ttf

Referenced from:
    src/components/quotes/QuotePDF.jsx (Font.register block)

License: Roboto is Apache License 2.0 — free for commercial use
including invoice / PDF generation / redistribution, no attribution
required in the output.

Source: https://fonts.google.com/specimen/Roboto (static/ subfolder).
